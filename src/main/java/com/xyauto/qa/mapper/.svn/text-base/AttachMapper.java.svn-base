package com.xyauto.qa.mapper;

import com.xyauto.qa.entity.Attach;
import com.xyauto.qa.util.BaseMapper;

public interface AttachMapper extends BaseMapper<Attach> {
	void saveAttach(Attach attach);

	int deleteById(long id);


	int saveAttachPicture(Attach attach);

}